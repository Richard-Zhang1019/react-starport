export type metaDataItem = Record<number, Record<string, any>>

export type proxyListItem = Record<number, { current: HTMLElement }>

export type landedMapItem = Record<number, (value: boolean) => void>

export {}
